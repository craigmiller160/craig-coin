import { Blockchain } from '../../src/chain/Blockchain';

describe('Blockchain', () => {
    it('gets read-only chain', () => {
       throw new Error();
    });

    it('addBlock', () => {
        throw new Error();
    });
});