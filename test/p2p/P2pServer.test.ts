export {};

describe('P2pServer', () => {
	it('addConnectedSocket', () => {
		throw new Error();
	});

	describe('updateSockets', () => {
		it('same length', () => {
			throw new Error();
		});

		it('new length', () => {
			throw new Error();
		});
	});

	it('listen', () => {
		throw new Error();
	});
});
