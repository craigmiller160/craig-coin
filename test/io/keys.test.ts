export {};

describe('io keys', () => {
	describe('keyFileExists', () => {
		it('does exist', () => {
			throw new Error();
		});

		it('does not exist', () => {
			throw new Error();
		});
	});

	it('loadPrivateKey', () => {
		throw new Error();
	});

	it('savePrivateKey', () => {
		throw new Error();
	});
});
