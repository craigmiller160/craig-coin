import { genesisBlock, mineBlock, hashBlock } from '../../src/block/blockUtils';

describe('blockUtils', () => {
    it('genesisBlock', () => {
        throw new Error();
    });

    it('mineBlock', () => {
        throw new Error();
    });

    it('hashBlock', () => {
        throw new Error();
    });
});