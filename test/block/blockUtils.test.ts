
describe('blockUtils', () => {
    it('genesisBlock', () => {
        throw new Error();
    });

    it('mineBlock', () => {
        throw new Error();
    });

    it('hashBlock', () => {
        throw new Error();
    });
});